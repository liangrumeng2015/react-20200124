import React,{useState,createContext,useContext} from 'react';

function AbcChild(){
    // const {value} = useContext(CountContext)
    return(
        <div>
            子组件 
        </div>
    )
}
export default AbcChild;